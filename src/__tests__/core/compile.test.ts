describe('compile', () => {
  it.todo('reads manifest to find uncompiled raw entries');
  it.todo('batches raw entries for parallel LLM calls');
  it.todo('constructs correct LLM prompt');
  it.todo('parses LLM response into wiki articles');
  it.todo('writes articles with YAML frontmatter and backlinks');
  it.todo('updates manifest with compiledAt timestamps');
  it.todo('skips already-compiled entries unless force=true');
});
